import { FilmactiveDirective } from './filmactive.directive';

describe('FilmactiveDirective', () => {
  it('should create an instance', () => {
    const directive = new FilmactiveDirective();
    expect(directive).toBeTruthy();
  });
});
